# Arrays

---

## LC 27 - Remove Element

**Pattern:** Fast & Slow Pointer

**Recognition**
- Remove elements in-place

**Core Idea**
- Read with `i`
- Write with `k`
- Return `k` because it is the new length.

**Mistake**
- Thought `return k` returned the array.

⭐ Revise Again
